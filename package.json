{
  "name": "hospital-rawson",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd client && npm install && npm run build"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.90.1"
  }
}
